{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cd /Users/yugagarwal105/Documents/allocation-agent && uv run pytest -q -p no:warnings >/tmp/pytest-gate.log 2>&1 || { echo 'COMMIT BLOCKED - the suite is failing:'; tail -6 /tmp/pytest-gate.log; exit 2; }",
            "timeout": 120,
            "statusMessage": "Running the suite before committing..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.py) cd /Users/yugagarwal105/Documents/allocation-agent && uv run ruff check \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
